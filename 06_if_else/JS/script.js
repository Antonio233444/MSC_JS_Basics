/*const Image = document.querySelector('.imageBlock');
let answer = prompt('Сколько тебе лет?');
answer = parseInt(answer);

if(answer <= 18) {
    Image.style.backgroundImage = "url(img/school.jpg)"
}
else if(answer <= 25) {
    Image.style.backgroundImage = "url(img/university.jpg)"
}

else (
    Image.style.backgroundImage = "url(img/job.jpg)"
)
*/


/*let number = prompt('Введите число');

if (number > 0) {
    console.log(number);
}
else {
    console.log(Math.abs(number));
}
*/


let number = prompt('Введите число');

( number > 0) ? console.log(number) : console.log(Math.abs(number));// ? - if,: - else;












