/*let people = ['Вася', 'Петя', 'Саша'];

console.log(people);

console.log(typeof people);// это объект;

console.log(people[2]);// в отличие от объекта,где для вывода используем точку или квадратные скобки,тут используем только квадртаные скобки;

people[1] = 'Юрий';

console.log(people);

console.log(people.length);

let bigArray = [ // такой массив называется многомерным
   [1,2,3],
   [true,false],
   ['a','b','c']
]
console.log(bigArray[0]);// будет выбран первый элемент этого большого массива,то есть первый малый массив внутри(с числами);
console.log(bigArray[0][2]);// будет выбрано число 3 в первом массиве внутри большого массива;

let bigObject = {
   names: ['Вася', 'Петя', 'Саша'],
   grades: [
      {
         intern: 1,
         junior: 2,
      },
      {
         middle: 3,
         senior: 2,
      }
   ]

}

console.log(bigObject.names[1]);// выбираем первый элемент из массива внутри переменной bigObject;
console.log(bigObject.grades[1].middle);
*/



