/*const Image = document.querySelector('.imageBlock');
let answer = prompt('Сколько тебе лет?');
answer = parseInt(answer);

(answer >=18 ) ? Image.style.backgroundImage = "url(img/job.jpg)" : Image.style.backgroundImage = "url(img/school.jpg)";//если да,то работа,если нет,то школа.
//Если условие истинно,выполняется первый блок кода(до двоеточия),если нет-то второй блок


/*let number = prompt('Введите число');

if (number > 0) {
    console.log(number);
}
else {
    console.log(Math.abs(number));
}
*/

let password = 12345;
let question = prompt('Введите пароль');
/*if(password == question) {
    alert('Пароль верный');
}
else {
    alert('Введите верный пароль');
}
*/

question == password ? alert('Пароль верный') : alert('Введите верный пароль');






